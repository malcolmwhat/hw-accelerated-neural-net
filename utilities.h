/*
 * utilities.h
 *
 *  Created on: May 16, 2017
 *      Author: mw
 */

#ifndef UTILITIES_H_
#define UTILITIES_H_

#define MAX(x, y) (((x) > (y)) ? (x) : (y))
#define MIN(x, y) (((x) < (y)) ? (x) : (y))

#endif /* UTILITIES_H_ */
